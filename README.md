# Pong game in go
Simple Pong implementation in go, for learning purpose.

To run test:
```
make test
```

To build (it will build binary in bin/pong):
```
make build
```

To build and run:
```
make run
```
